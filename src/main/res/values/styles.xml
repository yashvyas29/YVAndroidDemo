<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorActionBar</item>
        <item name="colorPrimaryDark">@color/colorStatusBar</item>
        <item name="android:textColorPrimary">@color/colorItemText</item>
        <item name="colorAccent">@color/colorItemText</item>

        <item name="android:textColorHint">@color/colorItemText</item>
        <item name="colorControlNormal">@color/colorItemText</item>
        <item name="colorControlActivated">@color/colorItemText</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.ProgressDialog" parent="ThemeOverlay.AppCompat.Dialog">
        <item name="android:textColor">@color/colorItemText</item>
        <item name="android:textSize">@dimen/text_size_normal</item>
        <item name="android:background">@color/colorActionBar</item>
    </style>>

    <style name="AppTheme.AlertDialog" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <item name="android:background">@color/colorActionBar</item>
    </style>>

</resources>
